# Site URL
NEXT_PUBLIC_SITE_URL=https://dominio.pt

# Authentication Configuration
# IMPORTANT: Change these values in production!
AUTH_SECRET=super-secret-key-change-me
ADMIN_PASSWORD=admin123
COZINHA_PASSWORD=123456

# Supabase Configuration
# Get these values from your Supabase project settings:
# https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_USE_SUPABASE_AUTH=true

# Session Timeout (minutes) - Automatically logs out inactive users
# Default: 30 minutes
NEXT_PUBLIC_SESSION_TIMEOUT_MINUTES=30

# =============================================================================
# PASSWORD POLICIES (Configure in Supabase Dashboard)
# =============================================================================
# Navigate to: Authentication > Policies
# Recommended settings:
#   - Minimum password length: 8 characters
#   - Require uppercase letters: Yes
#   - Require lowercase letters: Yes
#   - Require numbers: Yes
#   - Require special characters: Optional
# =============================================================================

# Supabase Configuration
# Get these values from your Supabase project settings:
# https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# =============================================================================
# SUPABASE SERVICE ROLE KEY (Server-side only - NEVER expose to client!)
# =============================================================================
# This key bypasses Row Level Security (RLS) and has full database access.
# Used for:
#   - Cron jobs (reservation reminders) that run without user context
#   - Server-side admin operations that need to bypass RLS
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Email Configuration (Resend)
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxx

# Email sender (must be verified domain or onboarding@resend.dev for testing)
FROM_EMAIL=reservas@dominio.pt

# TEST MODE: When set, ALL emails are redirected to this address
# Useful for development/testing. Remove or comment out in production!
TEST_EMAIL_OVERRIDE=your-test-email@example.com

# Restaurant notification emails (optional, defaults to reservas@dominio.pt)
RESTAURANT_EMAIL_1=reservas1@dominio.pt
RESTAURANT_EMAIL_2=reservas2@dominio.pt

# Resend Webhook Secret (for email tracking)
# Get this from: https://resend.com/webhooks
RESEND_WEBHOOK_SECRET=whsec_xxxxxxxxxxxx

# =============================================================================
# CRON JOB AUTHENTICATION
# =============================================================================
# Secret token to authenticate cron job requests (Vercel Cron)
# Generate a secure random string (e.g., openssl rand -hex 32)
# Used by: /api/cron/reservation-reminders
#
# Vercel automatically sends this as: Authorization: Bearer <CRON_SECRET>
# Configure in Vercel Dashboard > Project Settings > Environment Variables
# =============================================================================
CRON_SECRET=your_secure_random_string


# Twilio SMS Configuration (for phone verification)
TWILIO_ACCOUNT_SID=your_account_sid_here
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_PHONE_NUMBER=your_twilio_phone_number_here


# Vendus account:
# https://www.vendus.pt/dashboard/settings/api

# Global API Key (used if location-specific keys not set)
VENDUS_API_KEY=your_vendus_api_key

# Vendus API URL (base URL for Vendus API)
VENDUS_API_URL=https://api.vendus.pt/v1

# Store and Register IDs
VENDUS_STORE_ID=your_store_id
VENDUS_REGISTER_ID=your_register_id